apiVersion: v1
kind: Service
metadata:
  name: banking-app
  labels:
    app: banking-app
spec:
  type: ClusterIP
  ports:
  - port: 8085
    targetPort: 8085
    protocol: TCP
    name: http
  selector:
    app: banking-app
