{
  "dashboard": {
    "title": "Account Data Operations",
    "tags": ["banking", "account", "monitoring"],
    "timezone": "browser",
    "panels": [
      {
        "id": 1,
        "title": "Account Repository Operations Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(method_timed_seconds_count{class=~\".*AccountRepository.*\"}[5m])",
            "legendFormat": "{{method}}"
          }
        ],
        "yaxes": [
          {
            "label": "Operations/sec",
            "format": "ops"
          }
        ]
      },
      {
        "id": 2,
        "title": "Account Operation Response Times (95th percentile)",
        "type": "graph",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, rate(method_timed_seconds_bucket{class=~\".*AccountRepository.*\"}[5m]))",
            "legendFormat": "{{method}} - p95"
          }
        ],
        "yaxes": [
          {
            "label": "Response Time",
            "format": "s"
          }
        ]
      },
      {
        "id": 3,
        "title": "Account Endpoint Error Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(http_server_requests_seconds_count{uri=~\".*account.*\",status=~\"5..\"}[5m])",
            "legendFormat": "{{uri}} - {{status}}"
          }
        ],
        "yaxes": [
          {
            "label": "Errors/sec",
            "format": "ops"
          }
        ]
      },
      {
        "id": 4,
        "title": "Account Database Query Performance",
        "type": "graph",
        "targets": [
          {
            "expr": "histogram_quantile(0.99, rate(jdbc_query_seconds_bucket{table=\"account\"}[5m]))",
            "legendFormat": "Query p99"
          },
          {
            "expr": "histogram_quantile(0.95, rate(jdbc_query_seconds_bucket{table=\"account\"}[5m]))",
            "legendFormat": "Query p95"
          }
        ],
        "yaxes": [
          {
            "label": "Query Time",
            "format": "s"
          }
        ]
      },
      {
        "id": 5,
        "title": "Account Record Count",
        "type": "stat",
        "targets": [
          {
            "expr": "account_count_total",
            "legendFormat": "Total Accounts"
          }
        ]
      }
    ],
    "refresh": "30s",
    "time": {
      "from": "now-6h",
      "to": "now"
    }
  }
}
