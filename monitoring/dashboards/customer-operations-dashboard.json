{
  "dashboard": {
    "title": "Customer Data Operations",
    "tags": ["banking", "customer", "monitoring"],
    "timezone": "browser",
    "panels": [
      {
        "id": 1,
        "title": "Customer Repository Operations Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(method_timed_seconds_count{class=~\".*CustomerRepository.*\"}[5m])",
            "legendFormat": "{{method}}"
          }
        ],
        "yaxes": [
          {
            "label": "Operations/sec",
            "format": "ops"
          }
        ]
      },
      {
        "id": 2,
        "title": "Customer Operation Response Times (95th percentile)",
        "type": "graph",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, rate(method_timed_seconds_bucket{class=~\".*CustomerRepository.*\"}[5m]))",
            "legendFormat": "{{method}} - p95"
          }
        ],
        "yaxes": [
          {
            "label": "Response Time",
            "format": "s"
          }
        ]
      },
      {
        "id": 3,
        "title": "Customer Endpoint Error Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(http_server_requests_seconds_count{uri=~\".*customer.*\",status=~\"5..\"}[5m])",
            "legendFormat": "{{uri}} - {{status}}"
          }
        ],
        "yaxes": [
          {
            "label": "Errors/sec",
            "format": "ops"
          }
        ]
      },
      {
        "id": 4,
        "title": "Customer Database Query Performance",
        "type": "graph",
        "targets": [
          {
            "expr": "histogram_quantile(0.99, rate(jdbc_query_seconds_bucket{table=\"customer\"}[5m]))",
            "legendFormat": "Query p99"
          },
          {
            "expr": "histogram_quantile(0.95, rate(jdbc_query_seconds_bucket{table=\"customer\"}[5m]))",
            "legendFormat": "Query p95"
          }
        ],
        "yaxes": [
          {
            "label": "Query Time",
            "format": "s"
          }
        ]
      },
      {
        "id": 5,
        "title": "Customer Record Count",
        "type": "stat",
        "targets": [
          {
            "expr": "customer_count_total",
            "legendFormat": "Total Customers"
          }
        ]
      }
    ],
    "refresh": "30s",
    "time": {
      "from": "now-6h",
      "to": "now"
    }
  }
}
